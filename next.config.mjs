/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // output: 'dist', // ← Generates static files
    typescript: {
        // ignoreBuildErrors: true,
    },
    // experimental: {
    //     appDir: true,
    //     serverComponents: false, // Disable server components
    // },
};

export default nextConfig;
